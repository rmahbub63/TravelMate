<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahbub.travelmateui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tour_plan_logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/tour_plan_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:label="@string/app_name" />
        <activity android:name=".LoginRegistrationActivity" />
        <activity
            android:name=".SearchPlaceManualActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".AllDistrictActivity" />
        <activity android:name=".MainOptionSelectActivity"></activity>
    </application>

</manifest>