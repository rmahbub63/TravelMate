<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahbub.travelmateui">

    <application
        android:allowBackup="true"
        android:icon="@drawable/tour_plan_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tour_plan_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SearchPlaceManualActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".AllDistrictActivity"></activity>
    </application>

</manifest>